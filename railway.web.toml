[build]
builder = "dockerfile"
dockerfilePath = "apps/web/Dockerfile"

[deploy]
startCommand = "npm start --workspace=@waw/web"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
